[project]
name = "plex-smb-rescanner"
version = "0.1.0"
description = "Trigger partial rescans of Plex libraries on SMB change notifications."
readme = "README.md"
requires-python = ">=3.13"
dependencies = ["smbprotocol>=1.15.0"]
